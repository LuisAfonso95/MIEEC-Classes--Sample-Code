


struct frac {
	int num, den; 
};

struct frac fraccao(int, int);

void mostra(struct frac);

struct frac soma (struct frac, struct frac);

struct frac somamult(struct frac[], int);

void irred (int &, int &);

void irredutivel( struct frac &);

void  reduzir( struct frac [], int);

bool fracigual(struct frac, struct frac);

int mdc( int, int);

struct frac divisao( const struct frac &, const struct frac &);

	
	

